version: '3.8'

services:
  api:
    build: .
    ports:
      - "5000:8000"
    environment:
      - DATABASE_URL=sqlite:///./database.db
    volumes:
      - ./data:/app/data
      - ./app:/app/app
    restart: unless-stopped
